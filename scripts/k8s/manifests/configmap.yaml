apiVersion: v1
kind: ConfigMap
metadata:
  name: kibo-config
  labels:
    app: kibo
data:
  kibo.json: |
    {
      "gateway": {
        "mode": "local",
        "bind": "loopback",
        "port": 18789,
        "auth": {
          "mode": "token"
        },
        "controlUi": {
          "enabled": true
        }
      },
      "agents": {
        "defaults": {
          "workspace": "~/.kibo/workspace"
        },
        "list": [
          {
            "id": "default",
            "name": "Kibo Assistant",
            "workspace": "~/.kibo/workspace"
          }
        ]
      },
      "cron": { "enabled": false }
    }
  AGENTS.md: |
    # Kibo Assistant

    You are a helpful AI assistant running in Kubernetes.
