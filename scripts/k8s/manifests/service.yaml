apiVersion: v1
kind: Service
metadata:
  name: kibo
  labels:
    app: kibo
spec:
  type: ClusterIP
  selector:
    app: kibo
  ports:
    - name: gateway
      port: 18789
      targetPort: 18789
      protocol: TCP
