import Foundation
import KiboKit

// Prefer the KiboKit wrapper to keep gateway request payloads consistent.
typealias AnyCodable = KiboKit.AnyCodable
typealias InstanceIdentity = KiboKit.InstanceIdentity
