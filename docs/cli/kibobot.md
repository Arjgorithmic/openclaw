---
summary: "CLI reference for `kibo clawbot` (legacy alias namespace)"
read_when:
  - You maintain older scripts using `kibo clawbot ...`
  - You need migration guidance to current commands
title: "clawbot"
---

# `kibo clawbot`

Legacy alias namespace kept for backwards compatibility.

Current supported alias:

- `kibo clawbot qr` (same behavior as [`kibo qr`](/cli/qr))

## Migration

Prefer modern top-level commands directly:

- `kibo clawbot qr` -> `kibo qr`
